---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: node
    tag: alpine

inputs:
- name: frontend

run:
  path: bash
  args:
  - -c
  - |
    cd frontend
    npm run test
